// Project-level Gradle settings. In modern AGP, plugin versions and repository
// configuration live here, so this file serves as the "project-level" config
// alongside the module-level build.gradle in this directory.
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "WearOS_Bridge"
